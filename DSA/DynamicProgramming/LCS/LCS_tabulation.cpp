#include<bits/stdc++.h>
using namespace std;

int lcs(string s1,string s2,int m,int n)
{   
    int memo[m+1][n+1];
    fill_n

}
a


int main ()
{
    string s1,s2;
    getline(cin,s1);
    getline(cin,s2);
    cout<<lcs(s1,s2,s1.length(),s2.length());
    return 1;

}